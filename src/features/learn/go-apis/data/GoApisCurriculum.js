// PolyCode - Go APIs interactive course
// 7 chapters, 21 lessons, Beginner to Advanced

const ACCENT = "#00add8";
export const STAGES = ["beginner", "intermediate", "pro", "advanced"];

function text(content, code) {
  return code ? { type: "text", content, code: { lang: "go", label: "Runnable Go example", content: code } } : { type: "text", content };
}

function lesson(chapterTitle, chapterColor, id, title, xp, summary, example, challengeTitle, description, starterCode, solutionCode, patterns, question, options, answer, explanation) {
  return {
    id, title, xp, chapterTitle, chapterColor,
    theory: [text(summary, example), { type: "quiz", question, options, answer, explanation }],
    challenge: {
      title: challengeTitle,
      description,
      starterCode,
      solutionCode,
      tests: patterns.map(([label, pattern], index) => ({ id: index + 1, label, keywords: [{ pattern }] })),
    },
  };
}

const goProgram = (body, imports = '"fmt"') => `package main\n\nimport ${imports}\n\nfunc main() {\n${body}\n}`;
const challenge = (body, imports = '"fmt"') => goProgram(`    ${body}`, imports);
const definitions = (chapter, color, items) => items.map((item) => lesson(chapter, color, ...item));

export const GO_APIS_CHAPTERS = [
  {
    id: "go-apis-foundations", title: "API Foundations", stage: "beginner", icon: "🔌", color: ACCENT,
    lessons: definitions("API Foundations", ACCENT, [
      ["go-apis-0-0", "What Is an API?", 15, "An API is a contract between a client and a service. Go's net/http package provides small, explicit building blocks for that contract.", `package main\n\nimport (\n    "fmt"\n    "net/http"\n    "net/http/httptest"\n)\n\nfunc hello(w http.ResponseWriter, r *http.Request) { fmt.Fprintln(w, "hello from an API") }\n\nfunc main() {\n    recorder := httptest.NewRecorder()\n    hello(recorder, httptest.NewRequest(http.MethodGet, "/hello", nil))\n    fmt.Print(recorder.Body.String())\n}`, "Write an API greeting", "Create a handler that writes hello from an API.", `package main\n\nimport (\n    "fmt"\n    "net/http"\n)\n\nfunc hello(w http.ResponseWriter, r *http.Request) { _ = fmt.Fprintln }\nfunc main() { http.HandleFunc("/hello", hello) }`, `package main\n\nimport (\n    "fmt"\n    "net/http"\n)\n\nfunc hello(w http.ResponseWriter, r *http.Request) { fmt.Fprintln(w, "hello from an API") }\nfunc main() { http.HandleFunc("/hello", hello) }`, [["Writes greeting", "hello from an API"], ["Uses response writer", "fmt\\.Fprintln\\(w"]], "What does an API contract describe?", ["Inputs and outputs", "CSS styles", "A compiler version", "A database index"], 0, "An API contract describes how clients call a service and what responses they can rely on."],
      ["go-apis-0-1", "HTTP Methods", 16, "HTTP methods communicate intent. GET reads data and POST commonly creates data. A handler should check the method before doing work.", goProgram('    fmt.Println("GET reads resources")\n    fmt.Println("POST creates resources")'), "Accept a GET request", "Print GET reads resources and POST creates resources.", challenge('fmt.Println("GET reads resources")'), goProgram('    fmt.Println("GET reads resources")\n    fmt.Println("POST creates resources")'), [["Includes GET", "GET reads resources"], ["Includes POST", "POST creates resources"]], "Which method normally reads a resource?", ["GET", "POST", "DELETE", "PATCH"], 0, "GET is intended for retrieving a representation."],
      ["go-apis-0-2", "Status Codes", 17, "Status codes make API results predictable. Use 200 for success, 201 for creation, 400 for invalid input, 404 for missing data, and 500 for unexpected failures.", goProgram('    fmt.Println("200 OK")\n    fmt.Println("201 Created")'), "Explain success statuses", "Print 200 OK and 201 Created from a valid Go program.", challenge('fmt.Println("200 OK")'), goProgram('    fmt.Println("200 OK")\n    fmt.Println("201 Created")'), [["Includes OK", "200 OK"], ["Includes Created", "201 Created"]], "Which status means a resource was created?", ["200", "201", "404", "500"], 1, "201 Created communicates that a request created a resource."],
    ]),
  },
  {
    id: "go-apis-routing", title: "Routing and Parameters", stage: "beginner", icon: "🧭", color: "#22c55e",
    lessons: definitions("Routing and Parameters", "#22c55e", [
      ["go-apis-1-0", "Path Routing", 17, "A router maps URL paths to handlers. ServeMux keeps route registration explicit and supports method-aware patterns.", goProgram('    mux := http.NewServeMux()\n    mux.HandleFunc("GET /users", func(w http.ResponseWriter, r *http.Request) { fmt.Fprintln(w, "users route") })\n    _ = mux', '"fmt"\n    "net/http"'), "Register a users route", "Print users route after registering a GET /users route.", challenge('fmt.Println("users route")'), goProgram('    mux := http.NewServeMux()\n    mux.HandleFunc("GET /users", func(w http.ResponseWriter, r *http.Request) { fmt.Fprintln(w, "users route") })\n    _ = mux', '"fmt"\n    "net/http"'), [["Uses ServeMux", "NewServeMux"], ["Registers users", "GET /users"]], "What is the job of a router?", ["Map requests to handlers", "Encrypt passwords", "Compile Go", "Store images"], 0, "A router selects the handler matching a request."],
      ["go-apis-1-1", "Query Parameters", 18, "Optional query values appear after a question mark. Read them with r.URL.Query and provide a useful default.", goProgram('    name := "developer"\n    fmt.Println("hello,", name)'), "Read a query default", "Print hello, developer when no name is supplied.", challenge('name := "developer"\n    fmt.Println("hello,", name)'), goProgram('    name := "developer"\n    fmt.Println("hello,", name)'), [["Uses default", "developer"], ["Prints greeting", "hello,"]], "Where are query parameters read?", ["r.URL.Query()", "r.Body", "w.Header()", "http.Server"], 0, "The URL query collection contains values after the question mark."],
      ["go-apis-1-2", "Path Values", 18, "Dynamic paths such as /users/{id} capture values. With a method-aware ServeMux, read them with r.PathValue.", goProgram('    fmt.Println("user=42")'), "Read a path value", "Print user=42 for a captured user ID.", challenge('fmt.Println("user=42")'), goProgram('    fmt.Println("user=42")'), [["Prints user", "user=42"], ["Uses path concept", "Path"]], "Which API reads a ServeMux wildcard?", ["r.PathValue", "r.URL.Query", "w.WriteHeader", "fmt.Scan"], 0, "PathValue reads a value captured by a ServeMux wildcard."],
    ]),
  },
  {
    id: "go-apis-json", title: "JSON Contracts", stage: "intermediate", icon: "{ }", color: "#3b82f6",
    lessons: definitions("JSON Contracts", "#3b82f6", [
      ["go-apis-2-0", "JSON Responses", 19, "JSON is a common API representation. Set Content-Type before encoding a value with json.NewEncoder.", goProgram('    fmt.Println("Content-Type: application/json")\n    fmt.Println(`{"status":"ok"}`)'), "Describe a JSON response", "Print a JSON content type and status response.", challenge('fmt.Println("Content-Type: application/json")\n    fmt.Println(`{"status":"ok"}`)'), goProgram('    fmt.Println("Content-Type: application/json")\n    fmt.Println(`{"status":"ok"}`)'), [["Sets JSON type", "application/json"], ["Returns status", "status"]], "What header describes JSON?", ["Content-Type", "Cookie only", "Go version", "File extension"], 0, "Content-Type tells clients how to interpret the body."],
      ["go-apis-2-1", "JSON Requests", 20, "Decode JSON request bodies into a struct. Decoder errors should become a 400 response instead of being ignored.", goProgram('    var name string\n    _ = json.Unmarshal([]byte(`{"name":"book"}`), &struct { Name *string `json:"name"` }{})\n    fmt.Println("decoded request")', '"encoding/json"\n    "fmt"'), "Decode a request", "Use encoding/json and print decoded request.", challenge('fmt.Println("decoded request")', '"fmt"'), goProgram('    var payload map[string]string\n    json.Unmarshal([]byte(`{"name":"book"}`), &payload)\n    fmt.Println("decoded request")', '"encoding/json"\n    "fmt"'), [["Uses JSON package", "encoding/json"], ["Decodes input", "Unmarshal"]], "Which package decodes JSON?", ["encoding/json", "net/time", "http/css", "fmt/http"], 0, "encoding/json provides Decoder and Unmarshal for structured data."],
      ["go-apis-2-2", "JSON Validation", 20, "Decoding JSON checks syntax, not business rules. Validate required fields before creating a resource.", goProgram('    name := "pen"\n    if name == "" { fmt.Println("name is required") } else { fmt.Println("valid product") }'), "Validate a product", "Reject an empty name and otherwise print valid product.", challenge('name := "pen"\n    if name == "" { fmt.Println("name is required") } else { fmt.Println("valid product") }'), goProgram('    name := "pen"\n    if name == "" { fmt.Println("name is required") } else { fmt.Println("valid product") }'), [["Validates name", "name =="], ["Returns valid result", "valid product"]], "Why validate after decoding?", ["JSON syntax does not prove domain rules", "To change methods", "To compile faster", "To open a socket"], 0, "Valid JSON can still violate required fields or business rules."],
    ]),
  },
  {
    id: "go-apis-middleware", title: "Middleware and Errors", stage: "intermediate", icon: "⚙", color: "#38bdf8",
    lessons: definitions("Middleware and Errors", "#38bdf8", [
      ["go-apis-3-0", "Middleware", 21, "Middleware wraps an http.Handler to add logging, authentication, headers, or request IDs before calling the next handler.", goProgram('    fmt.Println("middleware before handler")'), "Run middleware", "Print middleware before handler from a valid program.", challenge('fmt.Println("middleware before handler")'), goProgram('    fmt.Println("middleware before handler")'), [["Runs middleware", "middleware before handler"], ["Calls handler concept", "handler"]], "What does middleware wrap?", ["An http.Handler", "A JSON field", "A database row", "A Go module"], 0, "Middleware wraps an http.Handler and adds cross-cutting behavior."],
      ["go-apis-3-1", "Central Errors", 22, "Consistent errors help clients recover. A shared helper can write one JSON error shape and status code.", goProgram('    fmt.Println(`{"error":"missing"}`)'), "Write an API error", "Print a JSON error containing missing.", challenge('fmt.Println(`{"error":"missing"}`)'), goProgram('    fmt.Println(`{"error":"missing"}`)'), [["Returns error field", "error"], ["Includes message", "missing"]], "Why centralize API errors?", ["To keep responses consistent", "To hide all statuses", "To avoid handlers", "To replace JSON"], 0, "A shared error shape gives clients predictable recovery data."],
      ["go-apis-3-2", "Request Logging", 21, "Request logging records method and path. Middleware can observe every request without duplicating code in endpoints.", goProgram('    method := "GET"\n    path := "/items"\n    fmt.Println(method, path)'), "Log a request", "Print GET /items as a request log line.", challenge('fmt.Println("GET /items")'), goProgram('    fmt.Println("GET /items")'), [["Logs method", "GET"], ["Logs path", "/items"]], "Where is request logging commonly implemented?", ["Middleware", "A JSON tag", "A struct field", "go.mod"], 0, "Middleware can observe every request."],
    ]),
  },
  {
    id: "go-apis-security", title: "Authentication and Security", stage: "pro", icon: "🔐", color: "#f59e0b",
    lessons: definitions("Authentication and Security", "#f59e0b", [
      ["go-apis-4-0", "API Keys", 23, "API keys are credentials sent by clients. Read them from a header and reject missing or invalid keys with 401.", goProgram('    key := "secret"\n    if key == "secret" { fmt.Println("authorized") }'), "Check an API key", "Print authorized after checking the expected API key.", challenge('key := "secret"\n    if key == "secret" { fmt.Println("authorized") }'), goProgram('    key := "secret"\n    if key == "secret" { fmt.Println("authorized") }'), [["Checks key", "key =="], ["Authorizes", "authorized"]], "What should happen for missing credentials?", ["Reject the request", "Grant admin access", "Ignore it", "Print it to clients"], 0, "Missing or invalid credentials should be rejected."],
      ["go-apis-4-1", "Bearer Tokens", 24, "Bearer authentication places a token after Bearer in the Authorization header. Validate the prefix before using the token.", goProgram('    header := "Bearer abc"\n    if strings.HasPrefix(header, "Bearer ") { fmt.Println("token received") }', '"fmt"\n    "strings"'), "Check a bearer token", "Accept a Bearer header and print token received.", challenge('header := "Bearer abc"\n    if strings.HasPrefix(header, "Bearer ") { fmt.Println("token received") }', '"fmt"\n    "strings"'), goProgram('    header := "Bearer abc"\n    if strings.HasPrefix(header, "Bearer ") { fmt.Println("token received") }', '"fmt"\n    "strings"'), [["Checks Bearer", "HasPrefix"], ["Returns token", "token received"]], "What prefix identifies a bearer token?", ["Bearer ", "Token:", "Auth=", "Go/"], 0, "Bearer is the conventional Authorization scheme prefix."],
      ["go-apis-4-2", "CORS and Headers", 23, "CORS headers tell browsers which origins may call an API. Allow only origins your application intends to trust.", goProgram('    origin := "https://example.com"\n    fmt.Println("Access-Control-Allow-Origin:", origin)'), "Set an allowed origin", "Print an explicit Access-Control-Allow-Origin header.", challenge('fmt.Println("Access-Control-Allow-Origin: https://example.com")'), goProgram('    fmt.Println("Access-Control-Allow-Origin: https://example.com")'), [["Sets CORS header", "Access-Control-Allow-Origin"], ["Uses explicit origin", "example.com"]], "What does a CORS header control?", ["Browser cross-origin access", "JSON indentation", "SQL joins", "Go scheduling"], 0, "CORS controls browser permission for cross-origin responses."],
    ]),
  },
  {
    id: "go-apis-testing", title: "Testing APIs", stage: "pro", icon: "🧪", color: "#fb7185",
    lessons: definitions("Testing APIs", "#fb7185", [
      ["go-apis-5-0", "Handler Tests", 24, "httptest.NewRecorder and httptest.NewRequest test handlers in memory without opening a network port.", goProgram('    recorder := httptest.NewRecorder()\n    fmt.Println("status=200", recorder.Code)', '"fmt"\n    "net/http/httptest"'), "Test a handler", "Create an httptest recorder and print status=200.", challenge('recorder := httptest.NewRecorder()\n    fmt.Println("status=200", recorder.Code)', '"fmt"\n    "net/http/httptest"'), goProgram('    recorder := httptest.NewRecorder()\n    fmt.Println("status=200", recorder.Code)', '"fmt"\n    "net/http/httptest"'), [["Uses recorder", "NewRecorder"], ["Checks status", "status=200"]], "What does httptest avoid?", ["Opening a real port", "Writing Go syntax", "Encoding all JSON", "Creating a module"], 0, "httptest exercises handlers in memory."],
      ["go-apis-5-1", "Table-Driven Cases", 25, "Table-driven tests describe many inputs and expected outputs with one loop, making edge cases easy to add.", goProgram('    cases := []string{"valid", "invalid"}\n    for _, item := range cases { fmt.Println(item) }'), "Run table cases", "Create and loop over valid and invalid test cases.", challenge('cases := []string{"valid", "invalid"}\n    for _, item := range cases { fmt.Println(item) }'), goProgram('    cases := []string{"valid", "invalid"}\n    for _, item := range cases { fmt.Println(item) }'), [["Defines cases", "cases :="], ["Loops cases", "range cases"]], "What is a benefit of table-driven tests?", ["Many cases share one shape", "They remove assertions", "They require a database", "They run only in browsers"], 0, "Tables make expected behavior easy to scan and extend."],
      ["go-apis-5-2", "Contract Tests", 25, "Contract tests check observable behavior such as status, content type, and response fields, protecting clients from accidental changes.", goProgram('    status := 200\n    contentType := "application/json"\n    fmt.Println(status, contentType)'), "Check an API contract", "Print status 200 and application/json.", challenge('fmt.Println(200, "application/json")'), goProgram('    fmt.Println(200, "application/json")'), [["Checks status", "200"], ["Checks content type", "application/json"]], "What does a contract test protect?", ["Observable API behavior", "Private variable names", "Editor themes", "Go comments"], 0, "Contract tests preserve behavior clients depend on."],
    ]),
  },
  {
    id: "go-apis-production", title: "Production API Design", stage: "advanced", icon: "🚀", color: "#8b5cf6",
    lessons: definitions("Production API Design", "#8b5cf6", [
      ["go-apis-6-0", "Context and Timeouts", 26, "Request contexts carry cancellation and deadlines. Pass r.Context downstream so abandoned requests do not keep expensive work running.", goProgram('    ctx := context.Background()\n    fmt.Println(ctx != nil)', '"context"\n    "fmt"'), "Use a context", "Create a context and confirm it is available.", challenge('ctx := context.Background()\n    fmt.Println(ctx != nil)', '"context"\n    "fmt"'), goProgram('    ctx := context.Background()\n    fmt.Println(ctx != nil)', '"context"\n    "fmt"'), [["Creates context", "context.Background"], ["Uses context", "ctx != nil"]], "Why pass request context downstream?", ["To propagate cancellation", "To format JSON", "To register routes", "To change a URL"], 0, "Context propagates cancellation and deadlines."],
      ["go-apis-6-1", "Graceful Shutdown", 27, "A production server should stop accepting new work and allow active requests to finish. http.Server.Shutdown uses a context deadline.", goProgram('    server := &http.Server{}\n    fmt.Println(server != nil)', '"fmt"\n    "net/http"'), "Prepare graceful shutdown", "Create an http.Server and confirm it exists.", challenge('server := &http.Server{}\n    fmt.Println(server != nil)', '"fmt"\n    "net/http"'), goProgram('    server := &http.Server{}\n    fmt.Println(server != nil)', '"fmt"\n    "net/http"'), [["Creates server", "http.Server"], ["Prepares shutdown", "server != nil"]], "What does graceful shutdown protect?", ["Active requests can finish", "All requests are dropped", "JSON is encrypted", "Routes are renamed"], 0, "Graceful shutdown coordinates stopping new work with active work."],
      ["go-apis-6-2", "API Documentation", 26, "Good API documentation states methods, paths, inputs, status codes, and examples. Keep documentation close to the contract.", goProgram('    method := "GET"\n    path := "/health"\n    fmt.Printf("%s %s -> 200\\n", method, path)'), "Describe an endpoint", "Print GET /health -> 200 as a compact API contract.", challenge('fmt.Println("GET /health -> 200")'), goProgram('    fmt.Println("GET /health -> 200")'), [["Documents method", "GET"], ["Documents path", "/health"], ["Documents status", "200"]], "What belongs in API documentation?", ["Methods, paths, inputs, and responses", "Only the logo", "Only server CPU", "Editor shortcuts"], 0, "Documentation makes the API contract usable by clients."],
    ]),
  },
];

export const GO_APIS_LESSONS = GO_APIS_CHAPTERS.flatMap((chapter) => chapter.lessons.map((item) => ({ ...item, chapterId: chapter.id })));
export const GO_APIS_TOTAL_XP = GO_APIS_LESSONS.reduce((sum, item) => sum + item.xp, 0);
